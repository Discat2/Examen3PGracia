server.port: 8081
server.error.include-message: always

logging:
  level:
    root: INFO

spring:
  application:
    name: Producto-Application
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: Escuela
      auto-index-creation: true